const style = {
  row: {
    display: "flex",
    flexDirection: "row",
    flex: 1,
    width: "100%",
    alignItems: "center",
    justifyContent: "center"
  }
} as { [key: string]: React.CSSProperties };

export default style;
