const style = {
  container: {
    backgroundColor: "blue",
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
  },
  subContainer: {
    width: "50%"
  },
  textHeader: {
    fontSize: 30,
    color: "white",
    fontWeight: "bold",
    textAlign: "center"
  },
  textMain: {
    fontSize: 20,
    color: "white",
    textAlign: "center"
  }
} as { [key: string]: React.CSSProperties };

export default style;
